# Copy this file to .env and update values before running the app.
# DATABASE_URL must point to a PostgreSQL database.
# For local development, use localhost or your own database host.
# When running inside Docker Compose, the app container connects to the db service at host `db`.

DATABASE_URL=postgres://postgres:dev@localhost:5432/tasks
PORT=3000
STORAGE=postgres
